<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sturrd.sturrd">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launchit"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launchit"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:windowSoftInputMode="adjustResize"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".AuthActivity" />
        <activity android:name=".ProfilesRequest" />
        <activity android:name=".ChooseGenderActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/selector" />

        <activity android:name=".EmailSignin" />
        <activity android:name=".MainActivity" />
        <activity android:name=".DateLocActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".VerifyActivity" />
        <activity android:name=".ProfileActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>